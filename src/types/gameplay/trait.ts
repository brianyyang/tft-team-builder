class Trait {
  id: string;
  name: string;
  breakpoints: number[];

  constructor(id: string, name: string, breakpoints: number[]) {
    this.id = id;
    this.name = name;
    this.breakpoints = breakpoints;
  }
}
